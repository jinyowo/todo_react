import React from 'react';

/**
 * 순수 함수 형태로 리팩토링
 * @param text
 * @constructor
 */

const Title = ({text}) => (
    <div className="page-header">
        <h1>To do List</h1>
    </div>
);

export default Title;